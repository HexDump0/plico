# Plico

An open-source, local-first PDF toolbox.

## Development

Install dependencies and start the development server:

```sh
npm install
npm run dev
```

Useful checks:

```sh
npm run check
npm run lint
npm run build
```

Format the project with:

```sh
npm run format
```
